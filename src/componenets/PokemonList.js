import React,{useState,useEffect} from "react"
import Card from "./Card"
import Axios from "axios";

const PokemonList=(props)=>{

/*let pokemon=[]
useEffect(()=>{
url.map(url=>{Axios.get(`${url}`).then(response=>{pokemon.push(response)})
setDataPokemonArray(pokemon);
})
},[])

let pokemonData=[...dataPokemonArray];

console.log(pokemonData);*/


/*{array.map(data=>(<Card img={data.sprites.front_default}></Card>))}*/

return(
<div>

</div>
)

}

export default PokemonList;